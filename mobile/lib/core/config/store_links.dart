/// Store girişi kimlikleri. `review_service.dart` (mağaza puanlama) ve
/// `force_update_screen.dart` (zorunlu güncelleme) aynı değeri kullanır —
/// tek yerden güncellenir.
class StoreLinks {
  // TODO: İlk yayından sonra gerçek Apple App Store ID'siyle değiştirin.
  static const appStoreId = '6470000000';
  static const androidPackageId = 'com.bagajpark';
}
